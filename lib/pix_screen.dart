import 'package:flutter/material.dart';

class PixScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pix'),
      ),
      body: Center(
        child: Text('Tela do Pix'),
      ),
    );
  }
}